@import './colors.pcss';

.textButton {
	background: none;
	border: 0;
	cursor: pointer;
	padding: 0;
	margin: 0;
	font-weight: 600;
	font-size: 16px;
	color: var(--text);
	&:focus,
	&:active{
		border:0;
		outline:0;
	}
}